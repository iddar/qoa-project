FROM node:20-alpine

WORKDIR /app

COPY src/package.json ./

RUN npm install --legacy-peer-deps && npm install -g tsx

COPY src/ .

RUN npm install --legacy-peer-deps pg

EXPOSE 3000

CMD ["sh", "-c", "npx drizzle-kit migrate --config drizzle.config.ts && node --import tsx index.ts"]
